1. For both the server and the client:
   - Delete `package-lock.json`
   - `npm install`
   - `npm run build`
2. Start the server with `node path/to/server/build/index.js`
3. Start the client with `node path/to/client/build/index.js`
